run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - misspell
    - unconvert

issues:
  exclude-dirs:
    - testdata
    - bin
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
        - unused
